const profileDropDown = document.querySelector("#profile-section");

const profileDropDownContent = `<div class="profile-dropdown-content">
    <a href="profile.html">Manage Profile</a>
    <a href="Settings" </a>
</div>`;

const profileDropDownMenu = () => {
  profileDropDown.innerHTML = profileDropDownContent;
};

const profileDropDownHandler = () => {
  profileDropDown.addEventListener;
};
